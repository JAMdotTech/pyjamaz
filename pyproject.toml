[build-system]
requires = ["setuptools>=61", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "pyjamaz"
description = "Python JAM implementation"
readme = "README.md"
license = { file = "LICENSE" }

authors = [
    { name = "JAMdot Technologies", email = "info@jamdot.tech" }
]

requires-python = ">=3.8"
classifiers = [
    "Programming Language :: Python :: 3",
    "License :: OSI Approved :: GNU General Public License v3 (GPLv3)",
    "Operating System :: OS Independent"
]
dynamic = ["dependencies", "version"]

[project.optional-dependencies]
dev = [
    "coverage==5.3",
    "pytest>=6.1.2",
    "mkdocs",
    "mkdocs-material",
    "mkdocs-autorefs",
    "mkdocstrings",
    "mkdocstrings[python]"
]

[project.urls]
Homepage = "https://github.com/JAMdotTech/pyjamaz"

[project.scripts]
pyjamaz = "pyjamaz.cli:main"

[tool.setuptools.dynamic]
version = {attr = "pyjamaz.__version__"}
dependencies = {file = ["requirements.txt"]}

[tool.setuptools.packages.find]
where = ["."]
exclude = ["contrib", "docs", "tests", "test"]

[tool.setuptools.package-data]
pyjamaz = ["data/*"]

